import Vue from 'vue'
import VueRouter from 'vue-router'
Vue.use(VueRouter)
const routes = [
    {
        path: 'Htal',
        // component: () =>import('../')
    }
]

export default new VueRouter({
    routes
})
